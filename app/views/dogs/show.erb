<h1><%= @dog.name %></h1>

<h4>Age: <%= @dog.age %></h4>

<h4>Breed: <%= @dog.breed %></h4>

<h4>Dunder Mifflin Employees: </h4>
<ul>
<% @dog.employees.each do |employee| %>
  <li><%= link_to employee.name, employee_path(employee) %></li><br>
<% end %>
</ul>

<p><%= link_to "View All Dogs", dogs_path %></p>

<p><%= link_to "Edit", edit_dog_path(@dog) %></p>
<p><%= link_to "Delete", dog_path(@dog),
                         :data => {:confirm => "Are you sure?",
                                   :method => "delete"} %></p>
